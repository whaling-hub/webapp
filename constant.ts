export const SUPPORTED_CHAIN = {
  1: {
    network: '',
    icon: ''
  }
}

export const LOGIN_SIGN_MESSAGE = '';


export const SERVICE_API_URL = {
  bots: process.env.UPSTREAM_URL ?? 'https://service.whalinghub.app',
}

/**
 * Layout
 */
export const MAX_PAGE_WIDTH = '6xl';